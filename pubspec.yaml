name: prototype_brutto
description: "Il Prototipo Brutto — Un dungeon crawler in un solo file. Prefazione pratica al libro Guida Universale Game Development con Flutter & Flame."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'
  flutter: '>=3.22.0'

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.34.0

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
